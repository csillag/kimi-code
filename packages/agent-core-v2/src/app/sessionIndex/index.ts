/**
 * `sessionIndex` domain barrel — re-exports the session index contract
 * (`sessionIndex`) and its scoped service (`sessionIndexService`). Importing
 * this barrel registers the `ISessionIndex` binding into the scope registry.
 */

export * from './sessionIndex';
export * from './sessionIndexService';
